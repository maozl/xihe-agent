"""CLI mode — the interactive chat frontend (chat REPL, doctor). One of the
three run modes, peer of gateway/ and gateway/serve/; the process launcher
lives in app/, the shared runtime in core.context."""
