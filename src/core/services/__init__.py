"""Feature domains consumed from outside the runtime: external_agent
(claude/codex engine), store (capability catalog), agent_defs (specialist
YAML), diagnostics (doctor probes shared by all three modes)."""
